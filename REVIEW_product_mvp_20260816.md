# 제품·MVP 리뷰: Coreline GitHub 바이럴·후원 자동화 기획서

> 리뷰 대상: `coreline_github_viral_funding_automation_plan.md` (v1.0, 1,622줄)
> 리뷰 일자: 2026-08-16
> 관점: 제품/MVP (가치제안·범위·우선순위·성공지표·운영비용·실패확률)

---

## 0. 한 줄 결론

**계획은 "홍보가 부족해서 안 알려졌다"를 전제로 하지만, 계정 실데이터는 "만든 것에 대한 수요가 아직 검증되지 않았다"를 가리킨다.**
따라서 이 기획서의 핵심 산출물인 *관리자 콘솔*은 잘못된 병목을 자동화한다. 6~8주는 콘솔이 아니라 **저장소 1개를 실제로 런치해보는 데** 써야 한다.

---

## 1. 근거: `coreline-ai` 계정 실측 (2026-08-16, `gh repo list` 기준)

리뷰를 위해 실제 계정 상태를 측정했다. 이 숫자가 이 문서의 모든 판단 근거다.

| 항목 | 실측값 |
|---|---:|
| 전체 저장소 | **117** |
| 공개 / 비공개 | 83 / 34 |
| 생성 기간 | 2026-01-08 ~ 2026-08-12 (**7개월**) |
| 전체 Star 합계 | **119** |
| 전체 Fork 합계 | **4** |
| Star 3개 이상인 저장소 | **2개** (`kakao_coreline_bot` 7, `trading_stock_skills` 3) |

Star 분포:

```
0개 : 13개 저장소
1개 : 95개 저장소   ← 전체의 81%
2개 : 7개
3개 : 1개
7개 : 1개
```

**95개 저장소가 정확히 Star 1개**라는 분포는 유기적 유입으로 나올 수 없는 모양이다(자연 분포라면 0이 압도적으로 많아야 한다). 본인 self-star로 보는 것이 합리적이며, 그렇다면:

> **7개월 · 117개 저장소가 만들어낸 외부 Star는 약 15개, 외부 Fork는 4개.**

self-star 가정을 빼고 가장 보수적으로 읽어도 결론은 같다 — **어떤 저장소도 외부 견인력을 확보하지 못했다.**

### 1.1 계획이 지목한 대상 저장소의 실제 상태

| 저장소 | Star | Fork | License | 최근 업데이트 | 계획상 서술 |
|---|---:|---:|---|---|---|
| `memory_node_graph` | 1 | 0 | mit | 2026-08-15 | "Viral Readiness **91/100**, 즉시 출시 가능" |
| `dev-plan-skill` | 1 | 0 | mit | **2026-06-29** | "Viral Readiness 78/100" |
| `dev-plan-v2` | 0 | 0 | **없음** | 2026-08-16 | 계획에 언급 없음 |
| `mini-web-game` | 2 | 0 | other | 2026-08-15 | "Product Readiness 93/100" |

여기서 세 가지가 드러난다.

1. **준비도 점수는 수요를 예측하지 못한다.** `memory_node_graph`는 91/100 = "즉시 출시 가능"인데 외부 Star 0이다. 점수는 *README 위생*을 측정할 뿐 *누가 이걸 원하는가*를 측정하지 않는다. (→ §4.2)
2. **계획은 낡은 쪽을 홍보 대상으로 잡았다.** `dev-plan-skill`은 2026-06-29 이후 방치, 후속작 `dev-plan-v2`가 어제까지 개발 중이다. 그런데 `dev-plan-v2`는 **라이선스가 없어** 계획 자신의 §3 규칙상 `UNKNOWN` → "오픈소스 표현 금지"다. 즉 **현재 주력 프로젝트는 계획의 규칙상 홍보 불가 상태**다.
3. Show HN에 `dev-plan-skill`을 올렸을 때 방문자가 저장소에서 볼 것은 "두 달 전 마지막 커밋"이다. 이건 콘텐츠 생성기가 해결할 수 없는 문제다.

### 1.2 계획의 분류 엔진이 구조적으로 못 잡는 실제 결함

공개 저장소 83개 중 **18개(22%)가 계획 자신의 기준상 오픈소스로 홍보 불가**다.

```
라이선스 파일 없음  : 12개  → UNKNOWN
license = "other"   :  6개  → SPDX 미인식(SOURCE_AVAILABLE/PROPRIETARY 판정 필요)
공개 저장소 라이선스 : mit 59 / NONE 12 / other 6 / gpl-3.0 2 / apache-2.0 2 / agpl-3.0 1 / isc 1
```

더 심각한 것 — `X` / `X_org` **미러 쌍에서 라이선스가 서로 모순**된다:

| 공개 | 라이선스 | 비공개 원본 | 라이선스 |
|---|---|---|---|
| `flutter_simple_mirroring` | **mit** | `flutter_simple_mirroring_org` | **agpl-3.0** |
| `kotlin_smart_adaptive_sound` | **agpl-3.0** | `kotlin_smart_adaptive_sound_org` | **mit** |
| `node_aiweb_news` | **mit** | `node_aiweb_news_org` | **isc** |

같은 코드베이스로 보이는 쌍이 서로 다른 라이선스로 공개돼 있다. AGPL 원본을 MIT로 재공개했다면 실제 라이선스 위반 소지가 있다.

**이 결함은 계획의 분류 엔진이 아무리 정교해도 잡지 못한다** — 계획의 로직(§3)은 저장소를 *하나씩 독립적으로* 판정하기 때문이다. 반면 `gh repo list --json name,licenseInfo` 한 줄 + 정렬로 5분 만에 발견된다.

> 시사점: **라이선스 정리는 "엔진"이 아니라 "1회성 청소 작업"이다.** 그리고 이것이 지금 이 계정에서 콘솔 개발보다 우선순위가 높은, 실재하는 유일한 리스크다.

---

## 2. 가치제안 평가

계획의 가치제안(§29):

> "내 GitHub 저장소의 실제 코드와 문서를 분석해, 프로젝트별 바이럴 출시팩과 후원 연결을 자동으로 만들어주는 개인용 오픈소스 런치 콘솔"

### 2.1 자동화 손익분기 계산

자동화의 가치 = `(반복 횟수 × 회당 절약) − 구축비 − 유지비`

| 변수 | 현실적 추정 |
|---|---|
| 반복 횟수 | 연 **3~10회** (혼자서 진지한 런치를 월 1회 이상 하기 어렵다) |
| 회당 절약 | **1~2시간** (프론티어 LLM에 RepoBrief 프롬프트 붙여넣는 것 대비) |
| 연간 절약 | **10~20시간** |
| 구축비 | 9개 모듈 + 14개 테이블 + 2개 배포면 + 에셋 파이프라인 = **수백 시간** |
| 유지비 | GitHub/SNS API 드리프트, Next.js·Remotion 업그레이드 = **연 20~40시간** |

**연간 유지비(20~40h)가 연간 절약(10~20h)보다 크다.** 손익분기에 도달하지 않는 것이 아니라, **순손실이 매년 누적된다.** 이건 취향 문제가 아니라 산수다.

### 2.2 그럼에도 진짜 가치가 있는 부분

계획 안에는 자동화와 무관하게 값어치가 확실한 자산이 섞여 있다:

- **§3 라이선스 표현 규칙** — 이 문서 전체에서 가장 값진 1페이지 (→ §5)
- **채널별 글 구조 템플릿** (§10의 X/Threads/Show HN/DEV.to 골격)
- **README 후원 섹션 문안** (§14, 한/영 완성본)
- **준비도 항목 리스트** (§6 — 점수가 아니라 체크리스트로서)

이것들은 **코드가 아니라 문서/템플릿**이고, 유지비가 0에 가깝고, 쓸 때마다 값이 붙는다.

> **권고 한 줄: 콘솔(console)을 만들지 말고 킷(kit)을 만들어라. 그리고 먼저 한 번 실제로 런치해라.**

---

## 3. 사용자 흐름 평가

### 3.1 첫 가치 도달까지가 너무 멀다

계획 §28의 13단계 실행 순서에서 **1~9단계가 전부 인프라**이고, **첫 캠페인은 10단계**다.

```
1  저장소 동기화        ┐
2  라이선스 분류         │
3  memory_node_graph 분석│
4  dev-plan-skill 분석   │  ← 전부 선행 인프라
5  /support 페이지 생성  │     Time-to-first-value 6주+
6  GitHub Sponsors 신청  │
7  토스 QR 준비          │
8  쿠폰 암호화 수신함    │
9  FUNDING.yml Draft PR ┘
10 첫 바이럴 캠페인      ← 여기서야 처음으로 외부 세계와 접촉
```

**문제:** 1~9단계는 전부 "런치가 잘 될 것"이라는 미검증 가정 위에 세워진다. 10단계에서 반응이 없으면 1~9가 통째로 매몰비용이 된다.

**수정:** 첫 런치를 3주차로 당기면 이후 모든 결정이 실데이터 기반이 된다. 후원 인프라는 *유입이 생긴 뒤에* 붙이는 게 맞다 — Star 1개인 저장소에 후원 버튼을 다는 것은 전환율 0이면서 "가치 증명 전에 돈부터 요구한다"는 인상만 남긴다.

### 3.2 "자동화"가 새로운 노동을 만든다 (핵심 역설)

계획의 흐름은 `생성 → 검토 → 승인 → 게시`이고, 한 저장소당 **8개 채널 + 12종 산출물**(§10)을 한 번에 만든다.

승인 원칙 자체는 옳다(→ §5). 그런데 **검토해야 할 초안이 12개**면, 검토가 새 병목이 된다. 직접 쓰면 1개를 30분에 쓰는데, 생성기는 12개를 만들고 사용자는 12개를 읽어야 한다. 두세 번 돌려보면 "그냥 내가 쓰는 게 빠른데"가 되고, 이 시점에서 콘솔은 방치된다. **이것이 이 프로젝트의 가장 유력한 종말 시나리오다** (→ §9).

---

## 4. 치명적 문제 7가지

### 4.1 잘못된 병목을 자동화한다 (최우선)

7개월 117개 저장소 → 외부 Star ~15개. **생산량은 이미 충분하고도 남는다.** 부족한 것은 깊이와 차별성이다. 월 17개 저장소를 만드는 계정에 홍보 자동화를 붙이면, 검증되지 않은 프로토타입의 유통량만 늘어난다. 이건 개선이 아니라 증폭이다.

`kakao_coreline_bot`(7 star, 1 fork)만 유일하게 미약한 외부 신호가 있다. **계획이 이 저장소를 대상에서 누락한 것 자체가 징후다** — 계획은 "무엇이 실제로 반응을 얻었는가"가 아니라 "무엇이 홍보하기 좋아 보이는가"로 대상을 골랐다.

### 4.2 바이럴 준비도 점수는 예측 타당성이 없다

11개 항목 100점 만점(§6). 문제:

- **측정 대상이 틀렸다.** 라이선스 명확성 15점, GitHub Topics 5점, 후원 연결 5점 — 이건 *README 위생*이지 *바이럴 가능성*이 아니다. "후원 연결 여부"가 바이럴 준비도 항목에 들어간 것은 범주 오류다.
- **실증적 반증.** `memory_node_graph` 91/100 = "즉시 출시 가능", 실제 외부 Star 0.
- **가중치에 근거가 없다.** 왜 데모 15점이고 영문 설명 5점인가? 캠페인 n=3으로는 영원히 검증 불가.
- **가장 해로운 부작용:** 점수는 최적화 대상이 된다. 91점을 93점으로 올리는 작업(Topics 추가, 배지 추가)에 시간을 쓰게 되는데, 이 작업들의 실제 star 기여도는 0에 가깝다.

**대안:** 점수를 버리고 **Go/No-Go 체크리스트 5줄**로 대체.
```
[ ] 영문 한 줄 소개가 15초 안에 이해되는가
[ ] 상단에 동작 GIF 또는 스크린샷이 있는가
[ ] 5분 안에 실행 가능한가 (또는 클릭 한 번에 되는 데모가 있는가)
[ ] 라이선스가 SPDX로 명확한가
[ ] "나 말고 누가 이걸 왜 쓰는가"에 한 문장으로 답할 수 있는가   ← 유일하게 중요한 항목
```
마지막 항목에 답하지 못하면 나머지 4개가 만점이어도 런치하면 안 된다.

### 4.3 RepoBrief 근거 체인은 본인 저장소에 불필요하다

`claim-001` + `sourcePath` + `sourceStartLine`/`EndLine` + `confidence: 0.98` (§9).

이건 *타인의* 코드베이스를 *대량으로* 분석할 때 환각을 막는 아키텍처다. 그런데 **본인이 직접 작성한 저장소**다. "내 프로젝트가 하지 않는 일을 AI가 주장하는" 실패는 저자가 초안을 10초 읽으면 잡힌다. 줄 번호 추적 인프라를 구축·유지할 이유가 없다.

**유지할 것:** 규칙만 (`"최고" "가장 빠른" "혁신적" 금지`, `미검증 성능 수치 금지`). 이건 프롬프트 3줄이다.
**버릴 것:** Claim ID 스키마, 줄 범위 추적, confidence 점수, `content_draft_evidence` / `repository_evidence` 테이블.

### 4.4 후원 인프라가 기대수익 대비 과잉이고, 순 부채를 만든다

계획은 다음을 요구한다:
- AES-256-GCM + 코드별 nonce + auth tag + HMAC-SHA256 지문 + 5단계 생명주기(SUBMITTED→NOTIFIED→VIEWED→REDEEMED→PURGED) + 자동 삭제 잡
- 토스 송금 3단계 수동 대사(REPORTED/VERIFIED/REJECTED)
- 자체 `/support` 페이지 + Rate Limit + CAPTCHA + CSRF/XSS 방어

**기대 수익:** 외부 Star 총합 15개인 계정에서 현실적 후원 건수는 **0~2건**.

**그런데 만들어지는 것은 부채다:**
- 카카오 선물코드는 **무기명 금전 가치물**이다. 폼으로 받는 순간 타인의 자산을 수탁하게 된다.
- 유출·버그·삭제 잡 실패 시 실제 금전 손실 + 평판 손상.
- 암호화를 아무리 잘 해도 **"기능을 만들지 않는 것"보다 안전할 수 없다.**

**권고: 쿠폰 수신함을 만들지 마라.** 이메일 주소 한 줄 또는 오픈채팅 링크 하나로 대체하면 리스크가 0이 되고 구축비도 0이 된다. 실제로 쿠폰이 반복해서 오기 시작하면 그때 만들어도 늦지 않다(그리고 그때는 만들 이유가 생긴 것이다).

**토스 QR도 동일:** 후원 1건이 생긴 뒤에 붙여라. 자체 `/support` 페이지도 초기엔 불필요 — `FUNDING.yml`에 `github: coreline-ai` 한 줄이면 GitHub이 후원 UI를 대신 그려준다.

### 4.5 8채널 동시 배포는 안티패턴이자 평판 리스크

같은 런치를 X + Threads + LinkedIn + Reddit + Show HN + GeekNews + DEV.to + Disquiet에 동시 투하하는 것(§10)의 문제:

- **Reddit:** 다수 서브레딧이 자기홍보 비율 규칙(통칭 9:1)과 계정 이력 요건을 둔다. 커뮤니티 참여 이력 없는 계정이 자작 도구를 올리면 제거되거나 shadowban 대상이 된다. 계획은 "동일 본문 반복 게시 금지"만 다루고, **애초에 게시 자격이 없을 수 있다**는 더 어려운 사실은 다루지 않는다.
- **Show HN: 사실상 1회용 카드다.** 다듬어지지 않은 상태로 소진하면 두 번째 기회가 없다. 그런데 계획은 이걸 첫 배포 묶음에 넣는다. **Show HN은 마지막에 가야 한다** — 앞선 채널 피드백으로 README/데모를 고친 뒤에.
- **LinkedIn/Threads:** 개발자 도구 채택 기여도가 낮다.
- **채널이 늘수록 채널당 품질이 떨어진다.** 잘 쓴 2개 ≫ 자동 생성된 8개.

**§21 "향후 공식 API 게시" 표도 낙관적이다.** X는 write 티어 정책·가격이 자주 바뀌고, LinkedIn·Threads는 앱 심사를 거쳐야 한다(개인 개발자가 통과 못 할 수준은 아니나 리드타임과 마찰이 있다). 반대로 **실제로 열려 있고 무료인 Bluesky·Mastodon은 "선택"으로 밀려 있다** — 우선순위가 뒤집혀 있다.

### 4.6 로컬 9B 모델은 잘못된 최적화다

§23은 Ollama + 9B 양자화 모델 + JSON Schema 출력을 권장한다.

- 이 워크로드는 **연 3~10회** 실행된다. 토큰 비용 절감의 분모가 없다.
- 9B 양자화 모델이 쓴 영문 HN/Reddit 카피는 "LLM이 썼다"는 티가 난다 — **이건 다운보트의 직접적 원인**이고, 이 프로젝트가 피해야 할 바로 그 실패다.
- 사용자는 이미 Claude Code / Codex / grok CLI에 CLI 접근이 있다(API 키 불필요). 최고 품질 모델이 이미 손에 있는데 품질을 낮추는 선택이다.

**권고:** 로컬 모델 스택 삭제. 기존 CLI 호출.

### 4.7 성과 측정이 귀속(attribution)을 못 하고, 표본이 없다

- **가장 중요한 API가 빠져 있다.** 채널 귀속이 가능한 유일한 GitHub 표면은 Traffic API의 referrer 데이터(`/repos/{owner}/{repo}/traffic/popular/referrers`, 14일 롤링)다. 계획은 14개 테이블을 명세하면서 **이걸 언급하지 않는다.** 이게 없으면 "캠페인별 Star 변화"는 인과가 아니라 상관 추정에 불과하다.
- **표본이 없다.** 캠페인 3회, Star 증분 한 자릿수~수십 규모에서 채널 간 비교는 통계적으로 무의미하다. n=3을 위한 측정 인프라는 계측이 아니라 연극이다.
- **"Donation Conversion"은 분자 기댓값이 0~2다.** 지표가 아니다.
- **Star는 허영지표다.** 조회수에 비례할 뿐 사용에 비례하지 않는다. (→ §7의 대안 지표)

---

## 5. 계획이 잘한 것 (반드시 유지)

비판이 길어서 묻히면 안 되므로 명시한다. 아래는 **그대로 가져가야 한다.**

1. **§3 라이선스 표현 규칙 — 이 문서의 최고 자산.** proprietary 코드를 "오픈소스"로 부르면 HN/Reddit에서 공개적으로 파괴당하고, 이건 되돌릴 수 없다. `mini-web-game`(license=other)을 오픈소스 캠페인에서 분리한 판단은 정확하다.
2. **main 직접 push 금지 / Draft PR 기본** (§7, §24) — 정확하다.
3. **`생성 → 검토 → 승인 → 게시`, 무인 자동 공개 금지** (§1-3, §7 Phase 7) — 정확하다. 스팸과 평판 사고를 막는 올바른 기본값.
4. **공개 페이지에 GitHub 쓰기 권한·SNS 토큰 미보관** (§19) — 정확한 신뢰 경계 설계.
5. **멀티테넌트 SaaS를 만들지 않겠다는 판단** (§1, §22) — 범위 감각이 옳다. (다만 그 후 SaaS급 인프라를 설계한 것이 모순이다.)
6. **저장소 코드 실행 금지** (§8) — 정확하다.
7. **`mini-web-game`을 별도 캠페인 트랙으로 분리** (§4.3) — 옳은 세분화.

---

## 6. Must / Should / Later 재분류

### MUST — 6~8주 안에 반드시 (대부분 코드가 아니다)

| # | 항목 | 근거 |
|---|---|---|
| M1 | **라이선스 1회 청소** — 무라이선스 공개 12개 처리, `other` 6개 판정, **미러 쌍 3건 모순 해소** | §1.2. 실재하는 유일한 법적 리스크. `gh` 1회 + 수동 판단, 반나절 |
| M2 | **§3 표현 규칙을 1페이지 문서로 고정** | 계획 최고 자산. 코드 아님 |
| M3 | **대상 1개 선정** (2개도 많다) | 후보: `kakao_coreline_bot`(유일한 외부 신호) 또는 `dev-plan-v2`(현재 주력, 단 M1 선행 필수) |
| M4 | **README 영문 우선 재작성 + 상단 GIF 1개 + 동작하는 데모 URL** | 전체에서 ROI 최고. GIF는 Kap 수동 녹화 15분 |
| M5 | **GitHub Sponsors 신청을 1주차에 착수** | 심사·수납 설정 리드타임이 병목. 병렬화 목적 |
| M6 | **`FUNDING.yml` 한 줄(`github:`) + README 후원 섹션** (§14 문안 그대로), Draft PR 흐름 유지 | 자체 support 페이지 불필요 |
| M7 | **채널 2개 런치 → 피드백 반영 → 그다음 Show HN** | Show HN 1회용 카드 보호 |
| M8 | **측정 스크립트 1개** — Star 타임시리즈 + **Traffic referrers**(원 계획 누락분) → CSV | 30분 분량. `gh api` |
| M9 | **성공/중단 기준을 숫자로 사전 정의** | 계획에 완전히 부재 (→ §7) |

### SHOULD — 위에서 실제 성과가 나온 경우에만 (8주 이후)

- 채널별 **프롬프트 템플릿 팩** + 얇은 CLI (`brief <repo>` → 기존 Claude/Codex CLI 호출 → 채널별 `.md` 파일 출력). **DB 없음, 웹 UI 없음, 파일시스템만**
- 준비도 **체크리스트**(§4.2 5줄 버전) — 점수제 아님
- 16:9 대표 이미지 1종 자동 생성 (Satori 또는 Playwright). 나머지 규격은 만들지 않음
- Release 기반 업데이트 초안 (수동 트리거)
- **Bluesky / Mastodon** — API가 실제로 열려 있고 무료. §21에서 "선택"으로 밀린 것을 승격
- 저장소 인벤토리 JSON 캐시 (117개 규모라 조회가 느려지면)

### LATER / 만들지 말 것

| 항목 | 사유 |
|---|---|
| **Next.js 관리자 콘솔 + 14 테이블 DB** | 연 3~10회 사용. 유지비 > 절약 (§2.1) |
| **바이럴 준비도 100점 점수제** | 예측 타당성 없음, 실증 반증됨 (§4.2) |
| **RepoBrief Claim ID / 줄범위 / confidence 스키마** | 본인 저장소엔 불필요 (§4.3) |
| **암호화 커피쿠폰 수신함** | ❌ **만들지 말 것.** 기대수익 0~2건 vs 영구 수탁 부채 (§4.4) |
| **토스 QR + 3단계 수동 대사** | 후원 1건 발생 후 재검토 |
| **자체 `/support` 페이지** | `FUNDING.yml` 한 줄로 대체 |
| **에셋 6종 + 15~30초 소개 영상 자동 생성** | 범용 repo→video 생성기는 콘솔보다 큰 프로젝트 |
| **로컬 9B LLM 스택** | 품질을 낮추는 잘못된 최적화 (§4.6) |
| **X/LinkedIn/Threads 공식 API 자동 게시** | 심사·비용 마찰. 복사·붙여넣기로 충분 |
| **8채널 동시 배포** | 안티패턴 + 평판 리스크 (§4.5) |
| **다중 Reddit 커뮤니티 게시** | 계정 리스크 |
| **라이선스 분류 "엔진"** | 1회성 청소 작업(M1)이지 상시 시스템이 아님 |

---

## 7. 6~8주 현실적 MVP 권고안

**재정의: "콘솔"이 아니라 "런치 킷 + 실제 런치 2회".** 1~6주차에 웹 앱을 만들지 않는다.

| 주차 | 작업 | 산출물 | 코드량 |
|---|---|---|---|
| **W1** | 라이선스 청소(M1) · 표현 규칙 문서화(M2) · 대상 1개 선정(M3) · **Sponsors 신청 착수(M5)** | 인벤토리 CSV, 규칙 1p, 대상 확정 | 0 |
| **W2** | README 영문 재작성 · GIF 1개(Kap) · 데모 URL 배포(Pages/Vercel) | 런치 가능한 저장소 1개 | 0 |
| **W3** | **1차 런치: GeekNews + X(또는 Threads)** · 게시 전 Star 기록 · Traffic referrer 확인 | 실제 외부 반응 데이터 | 측정 스크립트(M8) |
| **W4** | 피드백으로 README·한 줄 소개 수정 · **2차 런치: DEV.to 또는 Reddit(자격 되면)** | 개선된 포지셔닝 | 0 |
| **W5** | `FUNDING.yml` + README 후원 섹션 Draft PR(M6) · Sponsors 승인 상태 확인 | 후원 경로 연결 | 0 |
| **W6** | **Show HN** (README가 2회 피드백으로 다듬어진 뒤) · 30일 측정 | 최대 노출 시도 | 0 |
| **W7** | **판정 게이트 (M9)** — 계속/중단 결정 | 의사결정 | 0 |
| **W8** | *조건부*: 게이트 통과 시에만, 실제로 아팠던 반복 지점만 자동화 | 템플릿 팩 + 얇은 CLI | 소량 |

**핵심 차이:** 원 계획은 6주차에 인프라가 완성되고 런치는 아직 0회다. 이 안은 **3주차에 첫 런치**가 나가고, 이후 모든 결정이 실데이터 기반이 된다.

### 왜 저장소 1개인가

117개 중 1개를 고르는 것은 낭비처럼 보이지만, 반대다. 7개월간 117개를 병렬로 민 결과가 외부 Star 15개다. **같은 전략의 확장판은 같은 결과를 낸다.** 1개를 외부인이 실제로 쓰는 상태까지 끌고 가는 경험이 없으면, 자동화할 대상 자체가 존재하지 않는다.

---

## 8. 성공지표 재설계

원 계획은 *무엇을 측정할지*만 있고 ***얼마가 성공인지가 없다.*** 목표 숫자와 중단 기준이 없는 계획은 프로젝트가 아니라 무기한 취미다.

### 1차 게이트 (W7에 판정)

| 지표 | 임계값 | 성격 |
|---|---:|---|
| 실제 게시 완료한 캠페인 | ≥ 2회 | 실행 |
| 대상 저장소 외부 Star 증가 | **≥ 30** | 관심 |
| Traffic referrer로 식별된 유입 채널 | ≥ 1개 | 귀속 |
| **제3자 사용 증거** (타인 Issue/Discussion/의미 있는 Fork) | **≥ 1건** | **채택** |

### 가장 중요한 지표: "제3자 사용 증거"

Star는 조회에 비례하고, Issue·Discussion·커밋 있는 Fork는 **실사용에 비례**한다.
현재 계정 전체 Fork가 4개라는 사실이 이 지표의 중요성을 보여준다 — Star 119개가 있어도 실제로 써본 사람의 흔적은 거의 없다.

> **타인이 올린 Issue 1건이 Star 100개보다 의미 있다.** 계속 투자할지 판단하는 신호는 이쪽이다.

### 중단 기준 (사전 합의 필수)

```
2회 캠페인 후 "제3자 사용 증거" 0건 →
  홍보 자동화가 아니라 제품을 고쳐야 한다는 신호.
  콘솔·킷 개발을 중단하고 제품 자체로 돌아간다.
```

이 기준을 미리 못 박지 않으면, 반응이 없을 때 "홍보가 부족했다 → 더 자동화하자"로 흘러간다. 이 프로젝트의 가장 위험한 실패 모드다.

---

## 9. 숨은 운영비용 (계획이 0으로 가정한 것들)

| # | 항목 | 실제 비용 |
|---|---|---|
| 1 | **승인 리드타임** — Sponsors 심사·수납/세무 설정, SNS 앱 심사 | 계획은 "단계"로 표기하나 실제로는 **대기**. Phase 3 전체를 블로킹 |
| 2 | **검토 노동** — 8채널 × 초안 = 승인 자체가 새 노동 | 자동화가 노동을 만드는 역설 (§3.2) |
| 3 | **두 배포면** — private console + public page | 시크릿·배포·모니터링 파이프라인 2벌 |
| 4 | **Mac mini 상시가동 + Tailscale/Cloudflare Access** | 초기 수 시간 + 지속 유지 |
| 5 | **쿠폰 수탁** — 보관·삭제 잡·유출 대응 | **영구 부채.** 타인의 금전 자산 (§4.4) |
| 6 | **스키마 유지** — 14 테이블 마이그레이션 | 캠페인 10회를 위해 |
| 7 | **API 드리프트** — GitHub/SNS/Remotion/Next.js | 연 수 회 파손. 사용은 연 3~10회 |
| 8 | **평판 리스크** — Reddit/HN 1회 실수는 계정에 영구 | 자동화가 이 리스크를 **증폭**시킨다 |
| 9 | 도메인·호스팅 | 작지만 0은 아님 |

**7번이 결정적이다:** 연 3~10회 쓰는 도구가 연 수 회 고장 나면, 쓸 때마다 먼저 고쳐야 한다. 도구가 순부채가 되는 지점이다.

---

## 10. 실패 시나리오 (확률순)

| 순위 | 시나리오 | 확률 | 완화책 |
|---|---|---|---|
| **1** | **콘솔은 완성되나 캠페인은 2~3회에 그치고, Star 증가 미미, 후원 0원 → 방치** | **높음** | W3 첫 런치 선행. 인프라는 성과 확인 후 |
| **2** | **6~8주를 인프라에 소진하고 실제 런치는 0회** | **높음** | 원 계획 §28 순서(1~9 인프라)가 이 실패로 직행. 순서 반전 필수 |
| 3 | 자동 생성 티 나는 글로 Reddit/HN에서 부정 반응 → 첫인상 손상 | 중간 | 채널 2개, 프론티어 모델, 최종 수동 리라이트 |
| 4 | 쿠폰 폼 유출·버그로 타인 자산 손실 | 낮으나 **영향 큼** | **기능을 만들지 않는다** |
| 5 | Sponsors 승인·수납 설정 지연으로 Phase 3 블록 → 그 위 UI 무의미 | 중간 | W1 병렬 착수(M5) |
| 6 | 라이선스 모순(§1.2) 상태로 홍보 → 공개 지적 | 중간 | M1 선행. 계획이 유일하게 잘 방어하는 영역 |
| 7 | proprietary를 오픈소스로 표현 | 낮음 | §3 규칙이 이미 방어 |

**1번과 2번이 이 프로젝트의 실질적 기댓값을 지배한다.** 두 시나리오 모두 원인이 같다 — *외부 반응을 확인하기 전에 인프라를 먼저 짓는 것*.

---

## 11. 최종 권고

1. **기획서의 콘솔 부분(§20, §22, §23, §27)을 6~8주 범위에서 제외한다.** 삭제가 아니라 **성과 게이트 뒤로 이동**.
2. **§3 표현 규칙, §10 채널 템플릿, §14 후원 문안은 즉시 문서 자산으로 확정한다.** 이게 이 기획서의 실제 산출물이다.
3. **커피쿠폰 수신함·토스 QR·자체 support 페이지를 범위에서 뺀다.** `FUNDING.yml` 한 줄로 시작.
4. **저장소 1개를 골라 3주차에 실제로 런치한다.** 대상은 `kakao_coreline_bot`(유일한 외부 신호) 또는 `dev-plan-v2`(라이선스 정리 선행 조건).
5. **라이선스 청소(무라이선스 12개 + `other` 6개 + 미러 모순 3건)를 1주차에 끝낸다.** 콘솔보다 우선순위가 높은, 지금 실재하는 유일한 리스크다.
6. **W7 게이트에서 "제3자 사용 증거 ≥ 1건"을 못 넘기면 자동화를 중단하고 제품으로 돌아간다.**

> 이 계정에 지금 필요한 것은 **117번째 저장소를 홍보하는 시스템**이 아니라, **1개의 저장소를 남이 실제로 쓰게 만드는 경험**이다. 그 경험이 생긴 뒤에야 무엇을 자동화해야 하는지 알 수 있다.

---

**리뷰 문서 버전:** 1.0
**측정 기준일:** 2026-08-16 (`gh repo list coreline-ai --limit 300`)
**원본 기획서:** 수정하지 않음
