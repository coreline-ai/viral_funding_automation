# implement_20260816_211901.md

작성 일시: `2026-08-16 21:19:01 KST`

이 문서는 공개 저장소 3종 검증에서 확인한 콘텐츠 품질 문제 두 가지를 수정하는 범위를 고정한다.

## 개발 목적

1. 일반 패키지 링크의 일부 문자열을 `try`로 오인해 공개 데모로 선택하는 문제를 수정한다.
2. README에서 기능 항목을 찾지 못했을 때 짧은 글에 프로젝트 설명이 두 번 반복되는 문제를 수정한다.

## 개발 범위

- 데모 힌트를 독립된 단어·경로 구간으로만 판정
- npm 등 패키지 레지스트리 링크를 데모 후보에서 제외
- 명시적 데모가 없으면 GitHub 저장소 homepage를 사용
- 기능이 없거나 설명과 동일하면 짧은 글의 보조 기능 문단을 생략
- 실패 사례를 자동 테스트로 고정
- Express·Flask·ripgrep 실제 저장소 재검증

## 제외 범위

- README 전체 구조를 이해하는 범용 AI 추출기
- 기능 추출 규칙의 대규모 확장
- 공개 링크를 문서·데모·패키지로 분류하는 새 UI
- LLM API, 외부 의존성, 데이터베이스
- 웹 GUI 디자인과 서버 API 계약 변경

## 참조 문서

- [GUI·저장 복원 완료 계획](./implement_20260816_203309.md)
- [현재 사용법](../README.md)

## 과거 실패·교훈 참조

- 검색 상태: `완료`
- 검색 대상 scope: `src/content.mjs`, `tests/content.test.mjs`
- 검색 결과: `0건`
- disposition: 적용할 기존 Dev Lesson 없음

## 재현 근거

| 저장소 | 증상 | 영향 |
|---|---|---|
| `expressjs/express` | `[npm registry]`의 `registry` 안 `try`를 데모 힌트로 오인해 `https://www.npmjs.com/` 선택 | 공식 homepage `https://expressjs.com` 대신 부정확한 링크 게시 위험 |
| `pallets/flask` | 기능 0개일 때 설명을 `primaryFeature`로 다시 출력 | 짧은 글 문장 반복 |
| `BurntSushi/ripgrep` | 기능 0개일 때 설명을 `primaryFeature`로 다시 출력 | 짧은 글 문장 반복 |

## 공통 진행 규칙

- 각 Phase는 앞선 Phase 자체 테스트 완료 후에만 시작한다.
- 먼저 실패 테스트를 작성하고 기존 코드에서 실패하는지 확인한다.
- 두 문제와 직접 관계없는 파서·렌더러 구조는 변경하지 않는다.
- 기존 실제 데모 배지와 `codapi.org/try/...` 인식은 유지한다.
- 생성 파일 6종과 서버 응답 계약을 변경하지 않는다.
- 새 외부 의존성을 추가하지 않는다.

## Phase 상태 요약

- [x] Phase 1 완료 — 실패 테스트 고정
- [x] Phase 2 완료 — 최소 수정
- [x] Phase 3 완료 — 회귀 및 실제 저장소 재검증

## QA 관점

- [x] `registry`, `entry`, `country` 안의 `try`를 데모 힌트로 오인하지 않는다.
- [x] `Try demo`, `/try/`, `live-demo`는 계속 인식한다.
- [x] 이미지·배지 asset URL은 계속 제외한다.
- [x] 패키지 레지스트리는 명시적 문구가 있어도 데모로 선택하지 않는다.
- [x] 명시적 데모가 없으면 repository homepage로 fallback한다.
- [x] 기능이 없는 짧은 글에서 설명이 한 번만 나온다.
- [x] 실제 기능이 있으면 첫 기능 문단을 유지한다.
- [x] 생성 콘텐츠 3종이 모두 비어 있지 않다.
- [x] 기존 테스트가 계속 통과한다.

## 교훈 분류 요약

- [x] 발생 후보: 부분 문자열 기반 의도 판정과 fallback 재사용으로 실제 공개 저장소에서 품질 결함이 발생함
- [x] disposition: `plan-only` — 정확한 경계·레지스트리·fallback 회귀 테스트를 현재 콘텐츠 모듈에 직접 고정하며 별도 공용 Dev Lesson으로 확장하지 않는다.

## Phase 1. 실패 테스트 고정

### 목표

- Express 링크 오탐과 기능 없는 짧은 글 반복을 재현하는 회귀 테스트를 먼저 추가한다.

### 구현 태스크

- [x] `registry` 문자열이 포함된 npm 링크와 공식 homepage fixture 작성
- [x] npm 링크 대신 homepage가 선택되어야 하는 assertion 추가
- [x] 기능 없는 summary fixture 작성
- [x] 짧은 글에서 설명 출현 횟수가 1회여야 하는 assertion 추가
- [x] 실제 `/try/` 데모 링크 유지 assertion 추가

### 자체 테스트

- [x] 수정 전 신규 테스트가 두 결함을 정확히 재현해 실패
- [x] 실패 원인이 fixture 오류가 아닌 현재 선별·fallback 로직임을 확인

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 두 결함의 회귀 테스트 작성 완료
- [x] 실패 재현 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 최소 수정

### 목표

- 기존 정상 경로를 유지하면서 두 결함만 수정한다.

### 구현 태스크

- [x] 데모 힌트 정규식을 독립 토큰·URL 경계 기반으로 변경
- [x] 패키지 레지스트리 hostname 제외 함수 추가
- [x] 기존 asset URL 제외와 GitHub 링크 제외 유지
- [x] 기능 문단을 설명과 다를 때만 짧은 글에 포함
- [x] 출력 줄바꿈 형식 유지

### 자체 테스트

- [x] Phase 1 신규 테스트 통과
- [x] 기존 Live Demo 배지 테스트 통과
- [x] README 없는 기본 콘텐츠 테스트 통과
- [x] 콘텐츠 파일 6종 계약 유지

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 두 결함 수정 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. 회귀 및 실제 저장소 재검증

### 목표

- 전체 회귀와 실제 공개 저장소 3개에서 수정 효과를 확인한다.

### 구현 태스크

- [x] Express에서 공개 링크가 `https://expressjs.com`인지 확인
- [x] Flask 짧은 글에서 설명 중복 제거 확인
- [x] ripgrep 짧은 글에서 설명 중복 제거 확인
- [x] 세 저장소 모두 콘텐츠 3종과 GitHub 링크 유지 확인

### 자체 테스트

- [x] `node --check src/content.mjs`
- [x] `npm test`
- [x] Express·Flask는 실제 `/api/generate` 200, ripgrep은 GitHub API 한도 도달 후 현재 공개 raw README로 동일 렌더 경로 검증
- [x] 깨진 `undefined`, `null`, `[object Object]` 문자열 없음
- [x] git diff whitespace·시크릿 패턴 검사

### 이슈 및 수정

- [x] 세 번째 ripgrep 재검증에서 GitHub 비인증 API 한도(`remaining: 0`, reset `21:32 KST`)에 도달해 공개 raw README를 사용한 결정적 fallback 검증으로 전환

### 완료 조건

- [x] 두 실제 품질 결함 해결
- [x] 기존 기능 회귀 없음
- [x] 미해결 필수 결함 없음
- [x] 계획 체크박스와 실제 상태 일치

## 최종 완료 기준

- [x] Express가 npm registry 대신 공식 homepage를 사용
- [x] Flask·ripgrep 짧은 글 설명 중복 없음
- [x] 실제 `/try/` 데모 링크 유지
- [x] 기존 출력·서버·GUI 계약 유지
- [x] 전체 테스트 통과

## 최종 결과 요약

- Express: npm registry 오탐 제거, `https://expressjs.com` 선택, 설명 1회
- Flask: 기능 0개 상태에서도 짧은 글 설명 1회
- ripgrep: 현재 공개 README의 `/try/ripgrep/` 링크 유지, 짧은 글 설명 1회
- `registry`, `entry`, `country` 부분 문자열 오탐 방지
- 명시적 `Try package` npm 링크도 데모 후보에서 제외
- 기존 Live Demo 배지와 독립된 `/try/` 링크 인식 유지
- 전체 자동 테스트 `23/23` 통과
